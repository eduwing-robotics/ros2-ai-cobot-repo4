Session transition: see [handoff.md](handoff.md) for version-scoped evidence.
The September 11 v5 hold supersedes the September 9 failed candidate for that
bounded continuity question only; learned task success remains unproven.

## Active product checkpoint — September 13 takeover

- [x] Read main2bab180 handoff against current source and runtime; retain zero
  learned/gripper sends and UNKNOWN physical Pick. Root owns the existing product
  Goal again; Thin/Router experimentation is closed.
- [x] Independently assess temporal responsibility. Select scoped REOPEN as
  recorded in `design.md`: generation qualification and current execution
  admission are distinct; the current planning-entry/post-planning TTL is not a
  true first-action deadline. No deployed check has changed.
- [x] Implement and specify the prospective generation-context-bound scoped mode
  through the existing native caller/OneJob/PickupExecutor, preserving legacy
  contracts, original timestamps, task budget and all current-state/Scene gates.
  Integrated `6f8a1db` from independently reviewed `f97d733`.
- [x] Verify the controlled-clock placement discriminator and context/replay,
  expiry/cancellation, start/hardware/Scene and legacy rejection counterexamples
  with focused producer/consumer tests, then independently review the cutoff.
  Core scoped checks passed (11 tests on integrated `6f8a1db`); cleanup-error
  preservation `8ca1d88` passed 14 scoped tests. The independent finding that
  appended cleanup metadata diverged from the persisted canonical diagnostic
  was corrected by `ffc00fd` (owner `0700a2c`): retain before persistence/hash.
  Combined affected producer/Web/Collection checks passed 80 tests in 22.208s.
  These scoped CPU checks do not qualify hardware effects or task success.
- [x] Retain original SOURCE/DESTINATION episode-instruction provenance through
  the normal scoped learned caller, not only the bound Collection campaign.
  `eefc86a` derives the binding from validated current jobs and the persisted
  region registry, checks the scoped task grant and retains it in preapproval.
  Independent review found no issues; wrong-direction grant and injected public
  binding rejected before worker startup. Existing task/region contracts are
  reused without historical preapproval or another execution/approval owner.
- [ ] Use a fresh qualified runtime and fresh run/grant/Scene binding for the
  next bounded learned pick-and-place and canonical diagnosis; retain qualified
  mechanical release/reset as separately attributed recovery/preparation, not
  a substitute for policy placement or proof of policy task success;
  consume its original-condition recommendation through existing Collection.
  September13 r2 now passes native generation, plan admission and recorder
  readiness (153.664ms inference,239.675ms oldest observation), but execute
  admission rejects `LEARNED_STALE_STATE`.14 diagnostic rows
  retained, no admitted episode or task success. Resolve the actual snapshot
  acquisition/consumption boundary; source/receipt age divergence was observed
  in a later read-only probe, not proven as the original rejection's cause.
  The returned execute-count0 was a fixed failed-start projection, not a retained
  native counter. Empty phase events are consistent with pre-send failure but
  do not independently restore physical continuity.
  The user's current A-position/workspace confirmation restored Scene315 through
  existing HUMAN/CAS recovery. Fresh r3 passed the prior freshness boundary, then
  failed `CONTACT_PROFILE_UNAVAILABLE`; its canonical lifecycle and diagnostic
  retain empty completed segments/rows and UNKNOWN outcome. Scene316 is UNKNOWN.
  The missing A yaw0 sheet is now published in the existing digest-resolved
  workspace_sheets directory, identical to the existing calibration input.
  Three focused model/sheet tests passed in0.008s. CPU replay then reaches the
  already-existing `CONTACT_MODEL_GEOMETRY` rejection: original URDF finger axes
  oppose the qualified opening-positive command convention. The candidate below
  remains unqualified; no limit, active model, data or approval was changed.
  r3 artifacts: `outputs/data_factory/runs/learned-scoped-return-20260913-r3/`.
  Next physical dependency is the existing model/contact replacement boundary,
  not another unchanged retry or model-quality threshold. Live camera/ROS owners
  were moved to visible Orca terminals at the user's request.
  r5 selected the normal opening-coordinate successor and reached recorder
  readiness, then failed initial `CONTACT_MODEL_BINDING` before dispatch:
  source URDF bytes were compared to native xacro-expanded runtime XML.
  Canonical full-model comparison now separates the native control extension
  and pins expanded bytes for subsequent contact consumers. Narrow stopped-run
  recovery accepts this initial prepare failure only with absent contact context
  and the retained prior correction chain. Combined model/launch/Scene/native
  continuation tests:21 PASS1.842s. Physical Pick remains UNKNOWN.
  r6 passes model binding but rejects initial contact-scene readback before
  dispatch. Actual native query matches floor/wall and cube geometry except
  quaternion w `0.9513600693627325` vs `0.9513600693627324`.
  Repair representation comparison with the existing1e-9 numerical convention,
  not a new physical tolerance; retain exact shape/ownership/permission checks.
  Artifacts: `outputs/data_factory/runs/learned-scoped-return-20260913-r6/` and
  `.agent-local/work/lerobot-fr5/r6-planning-scene-readback.json`.
  Integrated transport/Scene/native continuation checks:22 PASS1.121s,
  including world/attachment perturbations and initial cleanup failures. Actual
  saved three-object readback now passes CPU comparison; real motion is not
  established by this check. Stopped r6's added collision object was removed
  with exact before/after native readback; physical Scene continuity correction
  retained the original HUMAN observation rather than a new acknowledgement.
- [x] Correct native snapshot acquisition without extending freshness limits:
  `5f4b7ef` waits for original source stamps within the existing deadline
  (13 focused tests PASS0.672s; independent review found no issues). The opted-in
  v5 current-state reader also uses KEEP_LAST1 instead of10 on all four state
  topics; command result channels, recorder QoS and native latched completion/
  fault checks remain unchanged.11 affected tests PASS0.522s and independent
  scoped review accepted. Integrated read-only three-snapshot check passed;
  after0.8s idle, source ages were9.23/1.68ms versus prior99.54/92.62ms.
  Evidence: `.agent-local/work/lerobot-fr5/scoped-return-snapshot-followup-`
  `{source-aware,latest-qos,integrated-latest-qos}.json`. These observations do
  not reconstruct r2 or qualify physical Pick/stop. Queue-depth rationale:
  https://docs.ros.org/en/humble/Concepts/Intermediate/About-Quality-of-Service-Settings.html
- [x] Preserve failed-start lifecycle through the existing canonical diagnostic
  consumer (`0491ea2`); do not claim zero sends when post-readiness evidence is
  absent/malformed. Five affected persistence/Collection checks PASS1.710s.
  This is prospective only: no r2 lifecycle or Scene evidence was reconstructed.
- [x] Connect completed scoped-attempt/mechanical-terminal lineage to explicit
  original-condition validation advice without human FAIL fabrication. Focused
  software tests cover recommendation rejection/legacy compatibility and the
  native advice choose → draft update → compile consumer; real Pick, landing,
  data utility and actual next Collection remain unqualified. Final focused six
  tests PASS in 134.184s; independent re-review accepts exact single-grant
  history/budget and canonical nested-handoff/legacy-null coverage. No full-suite
  repetition or physical qualification is claimed.

Independent preparation uses existing owners: native capture-cost review and
diagnostic/recollection readiness do not require the temporal implementation,
GPU or robot ownership. They must not become a global prerequisite. Historical
checkboxes below are version-scoped evidence, not a new mandatory full audit.

## Current LeRobot pick-place connection

- [x] Preserve raw/processed chunk and originating observation-bound candidate before the existing proposal validator; replay a distinct read-only preexecution diagnostic without fabricating a terminal lifecycle or task failure. Focused producer/consumer regression covers storage failure, mutation, incomplete logs and unchanged outputs.
- [ ] Qualify actual transport consumption and teardown through the existing native full-chunk → OneJob → sole PickupExecutor path. The architecture comparison below selected this already-connected path; connecting the separate per-row Sync/plugin diagnostic path is not a prerequisite and must not create a second queue/publisher.
- [x] Preserve original source, destination and instruction direction through failed pick-place diagnosis into existing recommendation/authoring; integration `6ceca27` retains both resolver receipts and consumes normal paired authoring. This closes the software contract only; actual original-condition next-Collection consumption remains open below. Exact focused evidence is in the handoff.
- [ ] Compare the newly trained pick-place checkpoints on the same approved heldout observations/noise and qualify the selected output through plan-only before physical execution.
- [ ] Independently review the integrated plugin/diagnostic change and verify regressions according to actual impact: focused producer/consumer checks by default; whole-repository discovery for cross-owner integration or uncertain impact, not every correction or push. Preserve exact cutoffs and coverage; CPU tests are not real rollout success.

## Native processor readiness

- [x] Reproduce a loader-admitted state normalization bypass with installed saved processors and synthetic CPU inputs.
- [x] Reject incompatible state/action declarations and state filters at the existing native loader boundary.
- [x] Reject inline statistics that supersede validated saved tensors.
- [x] Verify rejection before model loading and preserve valid saved-processor behavior with focused tests.
- [x] Consolidate saved-artifact validation in Learning's canonical validator and verify native failure propagation before model loading.

## Native runtime ownership

- [x] Reproduce overlapping inference through separate finite consumers sharing one loaded policy.
- [x] Reject the competing consumer before shared model/processor reset and preserve sequential reuse after success or failure.

## Execution architecture qualification

- [x] Characterize native RPC-budget rejection without commands and falsify single-acquisition success as proof of continuous certificate renewal; retain the distinction from physical measurements in `design.md`.
- [x] Verify a coherent complete-frame publication/getter candidate against the matching manufacturer SDK decoder, including concurrent reads, original receipt retention and reconnect invalidation; build in isolation without device or deployment effects. Reproduce with the opt-in `test_sdk_snapshot` integration test; this does not qualify native execution or physical timing.
- [ ] Integrate a versioned successor separating current-state delivery, command-specific completion and soft clock-alignment quality; preserve archived readers and test normal continuation as well as genuine delivery/command failures before physical qualification.
- [ ] Qualify sustainable feedback certification and fault propagation, comparing total-budget correction with responsibility/scheduling correction before deployment; preserve source identity, original freshness and motion authority.
- [x] Compare the installed LeRobot rollout/Robot seam against the current full-chunk consumer, including transformed/sent evidence and teardown ownership. Independent architecture assessment at `ac9389a` selected the existing native full-chunk path as lower total cost: LeRobot still owns policy/processors/inference, while its per-row plugin path lacks the required full-chunk admission and actual execution connection. Source recheck at `df24f93` confirms `NativeSmolVLA` calls native `predict_action_chunk`, normal `run_job` consumes `_infer_native_program`, and plugin `FR5.send_action` remains `FR5_ACTION_NOT_AUTHORIZED`. This is a scoped architecture decision, not physical qualification; reconsider only on new consumer/cost evidence.

This bounded outcome preserves the continuing Rollout Goal. Actual
checkpoint admission, resource assignment, physical qualification and
condition-level task-effect/data-utility evidence remain separate outcomes.

## Offline latency and action fidelity

- [x] Independently reproduce remaining-horizon midpoint behavior on a time-linear CPU field and compare its exact ODE endpoint separately from native Euler reference.
- [x] Add a three-step local midpoint control with six counted expert evaluations to the existing offline native consumer, preserving saved processors and model ownership.
- [x] Verify paired inputs, actual installed sampling consumption and analytic counterexamples with focused CPU tests.
- [x] Record the competing async path, bounded cost, falsifier and Learning/root handoff in this change.
- [x] Consume an immutable admitted trained checkpoint and Learning-selected observation scope after root resource assignment.
- [ ] Compare warmed chunk cost, per-joint action deviation and peak memory; revise the candidate using measured tradeoffs before any deployed change.
- [ ] Connect any later authorized runtime change to actual lifecycle traces and task/data-utility evidence; numerical results alone do not finish this outcome.

## Finite held-target consumption

- [x] Verify the opt-in serialized-reference caller with stored unchanged 4032
  rows, per-row retiming, explicit integer-percent alternative, ordered native
  goals and retained raw/proposed/terminal provenance.
- [ ] Integrate the serialized-reference schema into the existing Quality phase
  event consumer without changing its plan/segment identity rules (root-owned).
- [ ] Preserve the root-owned terminal lifecycle source retention in the normal
  caller when integrating reference options, so Curator can rederive diagnostics
  from the existing run evidence rather than trust a detached projection.
- [ ] Qualify total native wait and goal overhead against the unchanged five-second
  chunk bound before claiming practical physical playback; retain the controller
  start/sampling and whole-task effectiveness limitations below.

- [x] Distinguish controller-reference targets from observed feedback using native source and primary controller documentation.
- [x] Add explicit bound held-target proposals consumed by existing OneJob, sole executor and ROS transport without modifying approved arm commands.
- [x] Expose the default-false held-target option through the native checkpoint-to-plan entrypoint, verified with saved processors and the existing planner consumer.
- [x] Retain fresh start/terminal observations in the canonical learned trace, preserving failure, cancellation and data-admission boundaries.
- [x] Replay held completion and failure cases with synthetic clients and actual ROS serializers; check limits, delayed snapshot, cancellation and collision samples.
- [x] Preserve distinct, exact-plan-bound subsegment identities through the existing event writer/validator and report/phase/joint/interaction consumers; verify [1,2,3] rows join exactly 6 and gripper windows are excluded from arm metrics.
- [ ] Root integrates the scoped change and reviews the exact plan before any separately authorized physical baseline.
- [ ] Establish actual FR5 target tracking, task effect and safe-reset evidence; CPU replay does not discharge physical qualification or continuing data-utility ownership.

Runnable CPU check (no ROS node, model, GPU, original dataset or robot effects):

```sh
direnv exec . python3 -m unittest tests.data_factory.rollout.test_finite_plan tests.data_factory.rollout.test_learned_transport tests.data_factory.test_motion tests.data_factory.test_motion_transport_execution tests.data_factory.rollout.test_evidence_boundary tests.data_factory.test_quality tests.data_factory.rollout.test_native_policy --durations 5
```

## Continuous model references: consumer compatibility

- [x] Distinguish hardware integer resolution from its raw-reference enqueue deadband using the actual source and a CPU-extracted predicate replay.
- [x] Compare timed seven-joint execution against serial exact references; retain their timing, completion and staged-source limitations.
- [x] Verify through existing finite inference and OneJob that continuous in-limit output and staged source reject without input rewriting or executor/recorder effects.
- [x] Prepare an unapplied, exact-source-bound hardware patch for same-generation completion, coherent read-cycle evidence and native-time gating; verify copied driver methods with the installed sampler on CPU.
- [x] Reject paused-controller observations through held execution and canonical trace consumers; replay the existing wall-time timeout and sole cancellation owner with frozen controller time.
- [x] Falsify equal scaling as start synchronization and unscoped generation/time as sufficient identity; retain activation generation zero as observation only.
- [x] Correct the isolated worker's known stop/error and supersession fence before MoveGripper; preserve original patch evidence and the in-flight cancellation limitation.
- [x] Bind held-target evidence to hardware incarnation, command generation, explicit bounded source-clock mapping and independent monotonic freshness in existing native transport/executor/trace consumers.
- [x] Prepare tracked native export/known-stop patch and verify actual extracted C++ methods plus ROS serializers on CPU, including stale completion, supersession and paused-time rejection.
- [ ] Root deploys the exact driver residual and measures the same-incarnation source clock mapping; verify actual DynamicJointState export before physical consumption.
- [ ] Prove controller-start coherence and coherent seven-joint physical sampling; held-target metadata does not discharge these continuous-consumer requirements.
- [x] Retain successful JTC terminal evidence while waiting for fresh expected native completion under the same owner and original phase deadline; verify later completion, queued callbacks, cancel/lease/deadline and late-snapshot fences on CPU.
- [x] Compare unified-controller interpolation with the configured mixed native samplers using actual serialized proposals; preserve the counterexamples to identical-waypoint equivalence and common-header start coherence.
- [x] Exercise prior-command versus feedback initialization and immediate staged-release sampling with the installed native sampler; distinguish sample compatibility from completed hardware stages.
- [x] Retain native JTC source/trajectory clocks and literal reported command output through existing snapshot and learned trace consumers; verify signed durations, unavailable/old output and malformed-record rejection with actual ROS serializers.
- [x] Prepare opt-in post-command source freshness at the native worker/read-cycle release owner, using the existing measured binding and framework hardware node; verify cached/fresh, expired/rebound, incarnation, stop/error and late-resume cases with actual extracted worker/write methods and ROS serializers.
- [ ] Root verifies real same-command hardware completion/arm resume and clock mapping after driver integration; JTC tolerance and CPU replay alone remain insufficient.
- [ ] Implement an explicitly bounded native continuous-reference consumer with original full output, consumed indices and staged-release compatibility retained; do not automatically truncate or snap.
- [ ] Verify its normal executor/trace consumers and failure/cancellation before any separately assigned physical qualification.

Focused CPU acceptance (the vendor predicate replay is separate analytical
source evidence, not a simulated complete hardware execution):

```sh
direnv exec . python3 -m unittest tests.data_factory.rollout.test_finite_plan --durations 5
```

Focused held hardware evidence acceptance (CPU only; no driver installation):

```sh
direnv exec . python3 -m unittest discover -s tests/data_factory/rollout
direnv exec . python3 -m unittest tests.data_factory.test_motion_transport_execution tests.data_factory.test_motion
```

## Native observation-to-plan bridge

- [x] Add bounded source-stamped state and two-camera capture to the sole motion child's existing protocol, reusing the recorder's image conversion and retaining absolute seven-joint feedback.
- [x] Consume native capture after admitted model load through `run_learned_plan_only`, reusing that child for exact-plan compilation; preserve the supplied offline-observation path.
- [x] Verify actual ROS serializers, stale/paused/future source rejection, missing input, cancellation and saved-processor-to-plan consumption without goals or recorder effects.
- [x] Separate immutable inference input-age evidence from fresh execution-state admission across approval delay; retain original input stamps and require the planning hardware incarnation/generation at initial dispatch.
- [ ] Connect the resulting exact-plan consumer to current source-clock deployment and continuous seven-joint controller timing before physical execution; no future output inherits its approval.

Focused native capture/consumer checks:

```sh
direnv exec . python3 -m unittest tests.data_factory.rollout.test_policy_observation tests.data_factory.rollout.test_native_policy tests.data_factory.test_run_job.RunJobTest.test_native_observation_failure_and_cancellation_close_the_same_child
```

## Frozen-plan execution freshness

- [x] Retain initial admission freshness rejection timing through the existing executor/OneJob/public plan-only failure response, with exact proposal binding, unchanged predicate, immutable replay and original-exception preservation. Focused CPU evidence only; actual post-inference stage cost and physical qualification remain open.
- [x] Preserve inference/plan-admission source-age checks while replacing post-approval input-age reuse with current full-state/controller/hardware admission.
- [x] Bind the first dispatch to the planning hardware incarnation/generation; leave offline plans without that evidence reviewable but non-executable.
- [x] Retain original JointState/JTC stamps and validate freshness again after native goal deserialization; reject stale, paused, rebound, superseded or out-of-limit state without sends.
- [x] Retain the raw chunk's start observation in the existing canonical trace and preserve held-command completion/transition validation; reject pre-send state failures before activation and consult native goal ownership before cancellation.
- [x] Forward the existing measured clock file through the native planner factory; verify delayed-approval replay, failure/cancel and actual ROS serializers on CPU.
- [ ] Root verifies the deployed publisher clock domains and measured hardware binding; CPU fixtures do not qualify the physical graph.
- [x] Connect explicit learned inputs through the normal CLI/session plan-only and live callers, preserving one child, exact approval and honest finite-probe provenance.
- [x] Bind requested checkpoint/device, measured clock mapping, recipe/profile camera mapping and rate into the exact proposal; reject incompatible scope or changed mapping before effects.
- [x] Move checkpoint revalidation before fresh capture under one reserved inference scope; isolate saved CPU processor storage and test same-inode file mutation with actual native loading primitives.
- [x] Complete a discarded, shape-matched native warmup before the normal caller creates its child or captures fresh inputs; preserve random state, cleanup and separate preparation timing.
- [ ] Qualify native-resolution conversion/transfer/inference/plan admission against the unchanged 0.3-second budget on root-assigned runtime resources; synthetic small images and model seams do not establish usability.
- [x] Preserve canonical cross-workspace v4 source/destination bindings through learned compilation alongside existing v2; delegate endpoint validation to the source owner and reject unknown/nested schemas. Correction ab41b19 passes 113 focused tests; independent immutable review reports no findings, 95 tests and 11 falsifiers passing. Fresh r5 inference passed source-age checks but encountered the pre-fix source-version rejection; end-to-end runtime admission remains unproven, as detailed in the handoff.
- [ ] Close continuous start/pause/full-row/staged-release consumption under the existing executor and authority owners.

## Complete learned task attempt (direction accepted; native qualification pending)

Priority for the open items below is the smallest real Pick loop: existing
collected data and trained checkpoint → bounded learned Pick → qualified
mechanical release/reset → retained success/failure diagnosis → actual targeted
recollection. Authority, handoff and failure-evidence preservation are enabling
work for that outcome, not separate completion claims. Independent safe work may
proceed in parallel; this priority does not impose a serial implementation order.
Broader framework migration or interface generalization is not a prerequisite.

- [x] Distinguish new finite chunk completion from task review in the native executor and normal caller; retain one recording transaction and lease until explicit terminal review/abort, preserving previously frozen programs.
- [x] Consume a run/plan/lease-bound fresh observation at that boundary through OneJob and the native serializers; reject active ownership, stale/late/cancelled observations and unchanged plan replay without new sends.
- [x] Bound native capture-image cache lifetime and keep image bodies out of canonical execution evidence; preserve operation conflicts, reject stale/retired replay and retain lease enforcement on retries.
- [x] Retain successful UNKNOWN scene updates in existing execution evidence for learned terminal/non-release failure; verify historical snapshot retention and unchanged conflict/write-failure behavior with the actual temporary scene store.
- [x] Preserve repeated chunk identities in shared phase streams through explicit exact-plan lookup and per-plan existing quality attributes/reports; reject malformed/cross-run/replayed bindings and prevent cross-plan terminal or row aliasing.
- [x] Retain prior exact plan envelopes, approvals and validated traces through same-run pending-candidate planning and explicitly approved replacement; preserve one lease/recorder, native start/terminal checks and existing report/diagnostic consumers.
- [ ] Connect native inference and pending-candidate preview to the normal task interaction loop without claiming per-chunk approval as autonomous task usability; resolve the bounded future-output authority and physical producer contracts first.

- [x] Resolve user intent: system-consumed bounded authority covers learned Pick plus qualified mechanical placement/reset; no additional personal grasp gate or learned Place prerequisite. This decision is not runtime qualification.
- [x] Implement native bounded-attempt authority through the existing OneJob owner, with exact generated-plan admission and unchanged task deadline, rather than repeated human chunk approval. Preserve inference/plan input-age checks separately from current execution-state admission through recorder preparation and held-segment completion; physical qualification remains below.
- [ ] Connect learned Pick to existing qualified mechanical placement/reset from current measured state; validate handoff/contact/illumination scope and preserve separate control-source, Scene and recorder evidence through the normal diagnostic/recollection consumers.
- [x] Implement restricted prospective continuous-reference coverage and predecessor-bound carried-envelope continuity in the sole executor, with explicit native v4 selected-command evidence and unchanged archived v1/v2/v3 readers, policy rows, generations and staged release timing.
- [x] Exercise the real contact producer and native serializers with explicit simulated geometry/hardware, retained stored 8000-step checkpoint outputs, supported continuation and rejected coverage/tuple/contact evolution. These tests establish software behavior only; the three buildable stored programs are not claimed as successful physical Picks.
- [x] Integrate exact v4 runtime-input production and protocol admission through the normal caller, then independently review the combined producer/consumer path. Preserve exact v3 approvals rather than treating protocol pins as minimum versions.
- [ ] Physically qualify the candidate aperture/contact geometry and actual runtime environment; retain unavailable live paths until real qualification exists. Demonstrate physical release and original-condition next-Collection consumption separately from model-based envelope feasibility.
- [x] Connect stopped learned-run failure/cancellation to transaction-bound native recorder retention and the existing lifecycle diagnostic. Verify temporary RGB/state/action persistence, stop-before-retain ordering, uncertain-cancel/response preservation, unchanged Collection abort/commit and independent training authority.
- [x] Release live-owned transaction staging only after complete native diagnostic publication and source/ownership verification; verify the next native recorder begin, retained committed bytes and no new historical payload reads at normal startup. Partial, uncertain and orphan-quarantined saves remain explicitly unresolved.
- [x] Connect reviewed native v4 original-transition evidence to the normal paired Collection draft: retain both resolver receipts and episode instruction endpoints, validate current calibration/sheet/region and motion preset, preserve current placement separately, and consume the exact pair through recommendation validation, existing update_draft pair intents, advice choice and native compilation. Software-only fixtures; historical missing destination evidence and rotated-release authoring remain explicit unsupported needs. Focused command: `direnv exec . python3 -m unittest tests.data_factory.test_collection_recommendation tests.data_factory.test_collection_acquisition tests.data_factory.operator.test_acquisition_advice tests.data_factory.rollout.test_evidence_boundary` plus the three preapproval/resolver tests in `test_run_job.py`.
- [ ] Verify original-condition next-Collection consumption through the canonical diagnostic, qualified mechanical terminal and Scene owners without semantic PASS fabrication, source-data mutation or a second recorder.
- [ ] Compare synchronous same-owner continuation against supervised exact-approved chunks, retaining intervention, total task duration and native gripper/start/prefix evidence.
- [ ] Demonstrate attributable complete-task success/failure and diagnostic linkage; finite prefix completion, solver metrics and synthetic replay do not satisfy this outcome.
- [ ] Conditionally adopt a minimal upstream/commercial-tool adapter when a concrete consumer or measured bottleneck justifies it and implementation plus maintenance cost is lower than duplicating the capability. Reuse existing owners and contracts; bring this forward only when it directly enables the Pick loop or removes evidenced recurring work. Otherwise leave it open, without committing to a new framework, product layer or interface family.
- [x] Resolve replacement intent: keep existing controller/state/action coordinates, original data/checkpoints and historical qualification immutable; reuse the native model-selection and exact-binding interfaces instead of per-consumer inversions.
- [x] Verify the geometry-only replacement against unchanged arm/action semantics and historical data/checkpoint consumption; preserve the original model/configuration. The 2026-09-13 bounded transfer in design.md binds successor A/B qualifications to unchanged physical Collection evidence, not new aperture measurements. Normal A/B resolver/contact and native model-selection tests: 9 PASS, 0.856 s; independent source/hash review found no consequential error.
- [ ] Deploy the exact successor model consistently and consume a fresh normal bounded learned plan. Retain current Scene/hardware/collision checks; qualification transfer alone does not establish physical learned Pick/release success.
- [x] Compare r7 execution granularity and result/observation ordering with installed LeRobot 0.6.1 and official async/RTC/JTC responsibilities; record the accepted responsibility correction in design.md. Retained r7 evidence shows 50 ARM plus 22 GRIPPER segments, not successful Pick.
- [ ] Replace normal learned row-terminal playback with native continuous consumption, reusing LeRobot inference/chunk handling and FR5 execution ownership. Connect actual owner-produced evidence without a second playback product. The proposed grouped-ARM mode is not adopted; preserve historical readers rather than adding an unused runtime mode.
  - September14 software checkpoint: native-FK-derived prospective occupancy now reaches the actual CPU geometry query with expanded off-center carried boxes, retained source/released world geometry and original-context-bound prior intent. Native queue selection/conditional cumulative ACK and nonwaiting initialized state reads are implemented; neither query/selection nor goal acceptance grants row consumption. Focused geometry/contact/transport/queue/normal-start/OneJob tests:189 PASS /10.075s, including installed CPU geometry and synthetic actuator clients, no actual commands or ROS initialization. Normal caller→owner checked commit/ACK and once-per-output generation eligibility remain open; controller start lead and physical outcomes remain UNKNOWN. Today's scope excludes physical commands. Do not add per-row observation TTL or infer a controller start margin from the policy period.
  - The public normal-grant caller now selects static `plan_stream` instead of finite inference/planning, retaining exact source/destination instruction scope. Until the commit consumer is connected, it explicitly returns `LEARNED_STREAM_COMMIT_UNAVAILABLE` before approval, recorder or motion; this is incomplete wiring, not a new physical-quality gate. Focused caller/instruction/selection/native-queue tests:25 PASS /0.074s, synthetic CPU only. Existing finite/Collection routes remain distinct; no physical rollout or full software-baseline completion is claimed.
  - Once-per-output native generation eligibility is now retained through selection/conditional ACK, with explicit configured-bound replay and no per-row dispatch TTL. Stale native merges roll back; failed-inference retry uses the newly sampled input and clocks. Focused producer/queue/selection/loaded-scope tests:76 PASS /3.364s, synthetic CPU only. Native repeated merge errors do not reliably set `engine.failed`; existing task deadline/stop ownership remains required. Caller→owner commit/submit/ACK integration is still open.
  - OneJob now exposes exact task-bound prepare/commit requests to the sole PickupExecutor. The owner retains the selected native rows, polls geometry without blocking current motion monitoring, revalidates current execution/Scene/Cell and the retained anchor, and submits the checked pair through the real transport seam. Native reference progress is exposed for conditional ACK; goal acceptance remains zero consumption. Pre-send candidate rejection preserves the current task/Scene, while attempted or uncertain sends use existing fault/drain. Public `run_live` producer→selection→commit→queue ACK remains open; the static caller still returns `LEARNED_STREAM_COMMIT_UNAVAILABLE`. Explicit synthetic epochs do not qualify a production start margin or physical behavior. Prospective occupancy remains a provisional collision-query representation, not grasp/release success or a second Scene/task model.
  - September14 stop boundary: finish the already-discovered owner correction and focused checks, commit/push this software checkpoint, then stop. No new robustness/replacement strategy or physical run today. Next caller work must reuse its existing loop and native producer: preserve queue membership through submit (zero-count ACK is not an identity check), select only the unconsumed prefix, and connect the normal owner terminal/drain boundary. The production epoch/lead and actual JTC handover remain UNKNOWN; neither a synthetic future epoch nor canceled-goal unit tests qualify physical behavior. These are open consumer obligations, not new implementations in this checkpoint.
  - Checkpoint verification: `direnv exec . python3 -m unittest tests.data_factory.rollout.test_finite_plan.NativeTaskPlanConsumerTest tests.data_factory.test_arm_stream.TransportActuatorStreamTest tests.data_factory.rollout.test_execution_state tests.data_factory.rollout.test_stream_progress tests.data_factory.test_one_job tests.data_factory.rollout.test_stream_selection` passed 89 tests /3.585s. The discovered false-fault path is corrected: exact per-actuator successor acceptance permits only that retiring predecessor's CANCELED/status5/code0 terminal without faulting current motion; unexpected aborts remain failures. Split child acceptance and unchanged predecessor/successor consumed counts are covered. These are synthetic CPU/native-message tests, including actual transport goal construction with fake actuator clients; no model/GPU/ROS-node/camera/robot effects or full-regression/physical-qualification claim.
  - Immutable `2b997f7` independently passed the same 148 focused tests / 9.505 s, including the installed CPU helper. No consequential finding in template/check/submit identity or current-motion independence. This review does not close normal caller auto-commit/ACK, prospective geometry applicability, start-time qualification or physical execution.
  - The transport now binds an immutable zero-epoch native template, full sampled-reference coverage with explicit contact-hypothesis assignments, task/contact/native-initialization and Scene identity to the asynchronous query. Its checked-submit seam fills only the common start epoch, derives a separate executable revision and retains that identity even on ambiguous native send failure; intervals/rows remain unchanged and templates cannot directly dispatch. Pending/rejected/mismatched checks do not fence current actuator motion. Focused `direnv exec . python3 -m unittest tests.data_factory.test_arm_stream tests.data_factory.test_native_geometry tests.data_factory.test_native_geometry_binary tests.data_factory.test_actuator_stream tests.data_factory.test_contact_request_geometry tests.data_factory.test_motion_transport_execution tests.data_factory.rollout.test_finite_plan.NativeTaskPlanConsumerTest tests.data_factory.test_one_job`: 148 PASS / 9.513 s, including actual installed CPU helper → checked native pair → mock actuator sends, with no invented acceptance/ACK. A new cancel test initially expected a synchronous native cancel call and was corrected to pump the existing polling path. No robot/model/GPU/ROS-node effects. Normal executor selection/auto-commit/queue ACK, prospective applicability and actual start-time qualification remain open; no magic controller lead or phase detector is adopted. PickupExecutor retains decisions, not the components' computation.
  - `e6b2264` independently passed 125 focused checks / 8.203 s with no consequential finding. The geometry dependency is now project-installed via `direnv exec . colcon build --base-paths src/fr5_motion_geometry --packages-select fr5_motion_geometry --cmake-args -DPython3_EXECUTABLE=/usr/bin/python3` (one package, 9.01 s). Fresh `direnv` resolves `install/fr5_motion_geometry`; default installed-binary tests pass 11 / 1.100 s. `.envrc`, global ROS/controller libraries and hardware were not changed. This closes dependency installation, not live Scene/model timing or physical readiness.
  - Normal OneJob start now connects full-Scene/model async reads and CPU helper initialization through the same transport. Native polling and cancel/drain continue during `OPENING`; only actual initialization completion sets `WAITING_FOR_POLICY`, with zero sends/ACKs. Private qualified floor/wall/source additions preserve other captured objects and settings; conflicting existing geometry is rejected without shared Scene writes. Focused capture/contact/ARM-port/normal-task/OneJob/stream-plan/transport suites: 125 PASS / 8.119 s. A new test initially expected synchronous cancel completion; it was corrected to observe the existing next-tick drain, not change production semantics. Normal revision/timing admission, policy submit/ACK and loaded-system verification remain open. Immutable predecessor `8526f4f` independently passed its 28 checks / 5.741 s with source/binary hashes matched and no consequential finding.
  - The task-owned `fr5_motion_geometry` CPU helper and `motion/native_geometry.py` now provide initialized full-Scene native collision/FK batches through a pollable adapter. Query preparation, IPC/CDR and contact classification are off the command-owner loop; timeout/teardown affect only the owned CPU process. Immutable sampled rows, exact init/query digests and caller binding are retained; mismatched/expired results are discarded, not reassigned. Private released geometry is a world object, with explicit ROS body types and unchanged top-contact rejection. Combined adapter, actual installed binary and contact tests: 28 PASS / 5.686 s, using `direnv exec . env FR5_NATIVE_GEOMETRY_BINARY=/tmp/fr5-motion-geometry-build-VLQhDP/install/fr5_motion_geometry/lib/fr5_motion_geometry/fr5_native_geometry python3 -m unittest tests.data_factory.test_native_geometry tests.data_factory.test_native_geometry_binary tests.data_factory.test_contact_request_geometry`. The candidate is temporarily built, not globally deployed; normal full-Scene acquisition, revision/timing commit, progress/ACK and live caller integration remain open. No ROS initialization, device/model/GPU/robot execution or full-regression claim. The helper loads only a CPU robot geometry model, not a learned policy.
  - Normal task start now reserves the existing native actuator port under the same OneJob recorder, scoped grant, current-state, Cell and Scene owner. Polling avoids legacy row completion; uncertain cancellation retains recorder/port ownership until native handle settlement. Current-state checks are shared with historical readers without requiring a held policy target. Focused current-state/start, OneJob, observation, transport and actuator suites: 123 PASS / 3.747 s, CPU/synthetic ports only. Policy revision submission, consumed-prefix acknowledgement and normal caller/diagnostic integration remain open; no goals were sent. User requested software preparation only until physical return.
    - Independent immutable `00a0ba1` review found exception-attached native events lost on polling failure and opened-port ownership released by missing/inactive status. Both are corrected: retain the last native event batch without masking the primary error; retain opened-port ownership until actual fence/drain/close. The same focused suites plus both falsifiers pass 125 tests / 3.802 s. No physical or policy-success claim follows.
    - The existing run diagnostic/persistence consumer now accepts retained normal task lifecycle results as diagnostic v2, without a finite proposal/trace. Source/destination, checkpoint, recorder and native evidence stay bound; physical stop, task effectiveness and data deficit remain UNKNOWN. Actual OneJob start/cancel/drain reaches this consumer in a synthetic-port test; pending recording cannot publish a retained terminal diagnostic. Combined normal-start, stream-plan, evidence, collection, run-job and OneJob suites: 155 PASS / 23.718 s. Archived finite v1 remains readable. Native v2-to-targeted-Collection interpretation is still unsupported and explicitly rejected, not misclassified as policy failure; normal command submission and physical qualification remain open.
      - Immutable `d59a1dd` review found absent recorder run/transaction identity accepted by diagnostic defaults. Native v2 now requires the existing recorder run binding and a nonblank transaction ID; missing/blank counterexamples and actual OneJob retention pass with the affected diagnostic/caller checks (25 PASS / 0.611 s). Historical finite v1 semantics are unchanged.
  - Normal task configuration now passes through `OneJob.plan_stream` to the same `PickupExecutor` and existing scoped task grant without a frozen action output, finite steps, precommit report or manufactured execution evidence. The detached plan binds canonical v4 SOURCE/DESTINATION, Scene and saved policy settings; shared native runtime-input validation also serves historical proposals. Focused `direnv exec . python3 -m unittest tests.data_factory.rollout.test_stream_plan tests.data_factory.rollout.test_finite_plan.NativeTaskPlanConsumerTest tests.data_factory.test_one_job tests.data_factory.rollout.test_finite_plan.FinitePlanTest.test_public_runtime_provenance_rejects_malformed_inputs_and_changed_clock_before_send tests.data_factory.rollout.test_finite_plan.FinitePlanTest.test_public_causal_plan_only_freezes_explicit_serialized_retiming tests.data_factory.rollout.test_finite_plan.FinitePlanTest.test_public_causal_plan_only_freezes_explicit_percent_representation`: 33 PASS / 0.934 s. No transport, recorder or Cell effects in the new configuration/grant consumer tests. Normal start/revision admission, action submission and downstream diagnostic consumption remain open; this is not physical readiness or a runnable rollout claim.
    - Independent immutable `ff6af14` review: no consequential findings, 10 focused tests PASS / 0.305 s; six re-digested grant-scope substitutions rejected. No physical/model effects or full-regression claim.
  - The existing observer now consumes the normal task's policy configuration, retaining lease/cancel/topic/freshness checks without motion-completion polling. Request-local contact geometry reuses qualified source/destination and installed MoveIt RobotState conversion: source world is retained, carrying/released proxies are model hypotheses, not measured phases or task success. Native `touch_links` is restricted to the carried proxy; released geometry retains native TIP collisions and permits only exact-query FK-supported side-jaw contact, never blanket top contact. Focused `direnv exec . python3 -m unittest tests.data_factory.test_contact_request_geometry tests.data_factory.test_robot_model_replacement tests.data_factory.rollout.test_policy_observation tests.data_factory.test_motion_transport_execution tests.data_factory.test_one_job`: 79 PASS / 6.672 s. Includes a CPU installed-MoveIt conversion/collision check, no ROS node/device. Normal revision admission must still consume these requests, check complete native collision/constraint results and supply exact-query FK; normal start and physical execution remain open.
  - Include the downstream normal diagnostic/recollection consumer in this migration: `build_run_diagnostic`, `_human_failed_chunk` and `_completed_scoped_terminal` currently consume finite trace/history semantics. Accept the new owner-produced stream record without fabricating finite completion; preserve historical readers and Collection authoring/execution. Keep observed outcome separate from data-deficit hypotheses. Recollection effects require approved retraining followed by matched-condition rollout/evaluation, not a second rollout of the unchanged checkpoint alone.
  - The sole executor now consumes the native stream fence/drain boundary: faults fence once, retain the primary error and discrete native facts, and keep BLOCKED ownership alive until fenced handles settle and the transport closes. Missing/unreadable ownership cannot authorize release; JSONL EOF and after-command faults cannot abandon pending handles. Feedback coalesces by revision/actuator/goal with explicit counts rather than growing indefinitely during a delayed stop. `direnv exec . python3 -m unittest tests.data_factory.test_motion_transport_execution tests.data_factory.test_arm_stream tests.data_factory.test_actuator_stream tests.data_factory.test_one_job tests.data_factory.test_motion.Test.test_fake_execution_holds_reset_and_faults`: 98 PASS / 4.112 s. No normal stream start/dispatch is enabled by this change; native handle settlement is not physical stop qualification. Normal start/admission/contact and diagnostic wiring remain open.
  - The former ARM-only port is now one logical `ActuatorStream` over existing ARM/gripper native clients in the same exclusive transport slot. Pair-wide admission precedes both sends; native attempt counts survive partial send failure, and positively completed children do not impose a wait-for-both replacement barrier. Focused `direnv exec . python3 -m unittest tests.data_factory.test_one_job tests.data_factory.test_motion_transport_execution tests.data_factory.test_arm_stream tests.data_factory.test_actuator_stream`: 84 PASS / 1.334 s, covering Collection lifecycle, transport exclusion and partial-failure/cancellation paths. Normal OneJob/LeRobot admission, executable-time binding, full 7D evidence consumption and driver coexistence/retargeting remain open. Source/CPU checks do not qualify physical execution or the installed controller's preemption behavior.
    - The pure native-pair builder preserves all processed 7D rows, adds only an explicitly non-policy initial anchor, and emits a common start plus unchanged period. The existing mixed native sampler confirms the first gripper row is otherwise skipped; it does not prove SDK delivery or physical timing. The same focused four-module command now passes 87 tests / 3.455 s, including actual ROS message construction and CPU C++ sampler execution. No live caller or Collection recipe changed; normal owner integration remains open.
    - Native-pair collision queries now use ARM-linear/gripper-next-point samples, retaining the existing whole-robot validity service and a detached exact pair/timing digest. A synthetic obstacle at the native mixed reference is rejected where unified 7D interpolation would check a different gripper position. Derivative/mismatched-time inputs reject before queries; no sends, retiming or Collection recipe edits occur. Verification: `direnv exec . python3 -m unittest tests.data_factory.test_arm_stream tests.data_factory.test_actuator_stream tests.data_factory.test_motion_transport_execution tests.data_factory.test_one_job tests.data_factory.test_motion tests.data_factory.rollout.test_finite_plan.FinitePlanTest.test_held_collision_sampling_covers_gripper_travel_and_feedback_bounds`: 113 PASS / 17.091 s. Two initial Scene-lock subtest failures also reproduced at unchanged parent babb390; their outdated first-chunk expectation now checks existing NO_DISPATCH preservation, while a dispatched predecessor still requires the Scene-write error. Normal owner submission, fence/drain and close/carry/open context remain open; sampled geometry does not qualify physical tracking/contact.
  - Before live rolling selection, stage and verify the official Jazzy JTC preemption result-delivery fix (`4ab98223`, released in 4.41.0). Installed 4.40.1 retains the old branch and can lose the predecessor result; do not compensate with fabricated completion or a second controller.
    - Staged 4.42.1 release-library verification passed the upstream `TestTrajectoryActions.preempted_goal_receives_aborted_result` test: 1 PASS / 2.035 s, using in-process fake joint interfaces and isolated ROS domain 211. Source-built 4.42.1 also passed: 1 PASS / 2.043 s. The first harness attempt failed because replacing `LD_LIBRARY_PATH` removed required vendor paths; successful runs prepend the candidate to the inherited environment. No installed library or live stack changed; deployment/physical qualification remain open.
  - Goal-bound native feedback now reaches the ARM port's event return alongside acceptance/cancel/terminal facts. A bounded latest sample preserves native source/reference time, goal UUID, receipt time and coalescing count; no policy cursor or physical completion is inferred. Diagnostic conversion failure reports unavailable feedback without canceling motion. Weak-reference callbacks prevent native rejected-goal registrations from retaining attempt/trajectory payloads. Focused command above: 46 PASS / 0.683 s. The normal task consumer and 7D ARM/gripper adoption join remain open.
    - The cumulative reference reducer now joins ARM/gripper endpoint progress without row-terminal waits, counting the initial anchor as zero rows. Atomic unchanged-prefix queue advancement tolerates actual native asynchronous append/compaction while retaining row/input/tensor identity. Real ROS feedback exposed NumPy UUID scalars; native event projection now emits JSON-compatible integer UUIDs. Focused command `direnv exec . python3 -m unittest tests.data_factory.test_arm_stream tests.data_factory.test_actuator_stream tests.data_factory.rollout.test_stream_progress tests.lerobot_plugins.test_acknowledged_queue tests.lerobot_plugins.test_native_async tests.data_factory.rollout.test_native_policy tests.data_factory.test_one_job tests.data_factory.test_motion_transport_execution`: 143 PASS / 5.741 s. CPU/native-library and synthetic producer evidence only; normal execution-owner selection, deployed driver behavior and physical outcome remain open.
    - The existing seam also preserves unequal horizons without a new strategy: an actual native producer's four synthetic rows can supply a two-row selected prefix to the pure native builder (three controller points including the non-policy anchor). Pair acceptance acknowledges zero; completed two-row reference progress leaves the two unsent prediction rows and their raw/processed values intact. `direnv exec . python3 -m unittest tests.lerobot_plugins.test_native_async tests.lerobot_plugins.test_acknowledged_queue tests.data_factory.rollout.test_stream_progress`: 34 PASS / 1.818 s. This is CPU framing/queue evidence, not adoption of short-horizon scheduling or physical admission.
  - A version-bound LeRobot `ActionQueue` adapter provides atomic detached snapshots and conditional cursor advancement while reusing native merge/get/clear. Stable chunk/row identities survive non-RTC compaction; stale generations and malformed cursor inputs cannot advance it. `direnv exec . python3 -m unittest tests.lerobot_plugins.test_acknowledged_queue tests.data_factory.test_arm_stream tests.data_factory.test_motion_transport_execution`: 55 PASS / 0.943 s. No runtime selection, new scheduler, RTC delay-policy change or full-row consumption claim follows. Task-lifetime inference, actual 7D reference attribution and final-feedback/result ordering remain integration work.
    - Independent review exposed native append failure changing raw queue storage before processed append failed. Correction `6703a08` rolls back the native tensor references/cursor and leaves metadata unchanged; the original immutable falsifier now passes (1 test / 0.002 s). No version or native merge policy change.
    - The installed native async producer works with SmolVLA and `RTCConfig(enabled=False)`; `supports_rtc()` is independent of enabled guidance. No capability patch is needed. A pinned fresh-engine seam installs the acknowledged queue before observations/resume, reusing the native producer thread and saved-processor interface rather than adding a scheduler. Actual native factory/SmolVLA methods with a synthetic sampler verify consumption during blocked inference, stable row identity across the next merge and pause's in-flight publication semantics: `direnv exec . python3 -m unittest tests.lerobot_plugins.test_native_async tests.lerobot_plugins.test_acknowledged_queue`, 14 PASS / 0.066 s. This is not a real model-load, normal task-lifetime integration or physical qualification; these remain open above.
  - `motion/policy_observation.py` separates bounded latest-source observation from actuation completion. The existing idle capture now uses that same observer; streaming polls neither spin/wait nor modify active motion. Source timestamps, RGB conversion and freshness checks are retained, with two latest image messages and explicit close. `direnv exec . python3 -m unittest tests.data_factory.rollout.test_policy_observation tests.data_factory.test_motion_transport_execution tests.data_factory.test_arm_stream tests.data_factory.test_run_job.RunJobTest.test_native_observation_failure_and_cancellation_close_the_same_child`: 53 PASS / 0.738 s. Normal task-scoped observation publication and producer/owner connection remain open; the observer itself provides no physical authority.
  - `NativeSmolVLA.prepare_task_inference` now owns the loaded policy and saved processors for one native asynchronous task scope, revalidating checkpoint bytes/resetting once before capture. Its canonical observation adapter preserves the baked UP view and native feature mapping; escaped callbacks reject, and native stop timeout retains the actual producer/model ownership with explicit incomplete-stop reporting. Even an early caller `engine.stop()` cannot erase the retained thread handle. Focused native producer/queue/loader/observation command (the four `tests.lerobot_plugins.test_native_async`, `test_acknowledged_queue`, `tests.data_factory.rollout.test_native_policy`, `test_policy_observation` modules) passes 42 tests / 0.450 s. Synthetic CPU evidence only; normal run_job selection, per-output input binding, 7D command adoption and physical task outcome are still open.
    - One real original12k model smoke now passes through the native task producer and saved processors using historical r7 diagnostic row 0: 50x7 finite output, generation 1, queue consumption 0, physical sends 0, producer thread exited and GPU released; process exit 0. Peak RSS 3,828,199,424 bytes. Local reproducible script/result: `.agent-local/work/lerobot-fr5/native-task-smoke-20260913{.py,-result.json}`, script SHA-256 `a83512307cf197f18b5b9ae0e5283a1d0b25330d7a1b013679a3083ae8d42cd3`, checkpoint tree `e4bd332c86c38749741ebf64f63780e4e041ee8b66e47a3f588103fe9f384d0c`. Historical source times were preserved, not renewed or admitted as fresh; this was one un-warmed, unseeded inference, not a latency benchmark, action-equivalence or physical qualification. Existing dirty plugin initialization remains disclosed in the result, not an immutable-archive claim. No controller/camera was opened and no data/training admission changed.
    - Per-output input binding is now implemented for pinned native non-guided async inference: immutable original observation digest/source timestamps follow the actually sampled mapping into each merged row. A/B notification races, failed inference (including an unwrapped successor), native tensor preservation and owned-input copying are covered by `direnv exec . python3 -m unittest tests.lerobot_plugins.test_acknowledged_queue tests.lerobot_plugins.test_native_async tests.data_factory.rollout.test_native_policy`: 41 PASS / 1.994 s. Independent review's stale-label counterexample is closed by clearing thread-local binding at native inference start. No native scheduler rewrite, model/GPU run, RTC-guided provenance claim or physical consumption follows; normal runner/7D owner integration remains open.
- [x] Join the retained action result to the actual checked native terminal observation under existing deadlines; otherwise valid endpoint mismatch stays pending, while hard faults remain immediate. Confirmed witnesses survive delayed consumption, timeout and cancellation without resampling or fabricated physical acquisition timing. Focused `direnv exec . python3 -m unittest tests.data_factory.test_motion_transport_execution tests.data_factory.rollout.test_finite_plan`: 135 PASS / 89.682 s; subsequent ARM-result canonical-reader assertions passed in `FinitePlanTest.test_later_native_completion_advances_once_with_original_deadline_and_targets` (1 test / 0.483 s). Stored r7 trace still validates unchanged as FAILED with completed rows [0,1]. Software-only correction, not continuous-runtime or physical Pick qualification.
  - Task-scoped `OneJob.observe_policy` now reads the existing transport observer while execution is active, under the existing run/grant/lease binding. Pending input is not a task failure; RGB does not replace execution evidence or renew the lease. The owner pumps ROS callbacks once with zero timeout, including between outputs, without waiting for an action result. Observation requests no longer drive the motion-completion tick; normal polling/heartbeats still own it. Cancellation retires cached observation bodies before idempotent retries. Focused `direnv exec . python3 -m unittest tests.data_factory.rollout.test_policy_observation tests.data_factory.test_motion_transport_execution tests.data_factory.test_arm_stream tests.data_factory.test_one_job tests.data_factory.rollout.test_native_policy tests.lerobot_plugins.test_native_async tests.lerobot_plugins.test_acknowledged_queue`: 117 PASS / 2.009 s. Source/CPU verification only: normal runner selection and continuous ARM/gripper consumption remain open; this API is not a physical execution qualification.
- [ ] Exercise the corrected normal consumer on a fresh bounded physical pick-and-place attempt, preserving diagnostic retention and separate policy pickup/placement and mechanical recovery/reset outcomes; do not treat grouped compilation or CPU success as physical qualification.
  - Native batch-query preparation now exposes an owned, once-bound contact classifier; the existing single-contact API retains its per-call validation. Repeated checks do not rehash the complete immutable task/context, while changed caller inputs cannot mutate the bound classifier and missing-FK/top pressing still reject. Contact geometry suite: 8 PASS / 4.627 s; expanded bound-classifier side/top assertions: 3 PASS / 0.203 s. This prepares the nonblocking geometry consumer; it does not yet install that consumer or authorize a native revision.
  - Pre-dispatch faults now preserve Scene without rewriting its bytes or foreign revisions; an explicit dispatch-attempt marker survives continuation and becomes ambiguous before transport entry. Missing ACK/history cannot claim zero effects, and Cell remains blocked. Focused Scene/dispatch/pending-consumer regressions: 12 PASS / 1.228 s. This does not establish object immobility after a dispatched movement.
  - Pending gripper telemetry no longer requires ARM suppression, but still cannot satisfy readiness/completion. `tests.data_factory.rollout.test_current_bracket`, `test_gripper_evidence`, and `tests.data_factory.test_motion_transport_execution`: 42 PASS, 2 opt-in SKIP / 20.223 s; explicitly selected coherent-SDK `CoherentSnapshotTest`: 4 PASS / 1.779 s. These are source/native-fixture/ROS-serialization checks without physical calls; native overlap deployment and normal continuous consumption remain open.
